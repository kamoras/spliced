// Client-side game defaults shared across modes.

// Harmonies-style cap: wrong row submissions before the answer is revealed.
// The daily puzzle gets this value from the API; Practice mode uses this.
export const MAX_GUESSES = 4;
